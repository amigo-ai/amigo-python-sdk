Run linting and formatting checks:
1. Run `ruff check src/ tests/` for linting
2. Run `ruff format --check src/ tests/` for formatting
3. Fix any issues found with `ruff check --fix` and `ruff format`
